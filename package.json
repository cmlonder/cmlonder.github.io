{
  "name": "cmlonder-site",
  "type": "module",
  "version": "0.0.1",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check && node scripts/check-build.mjs",
    "verify": "node scripts/verify-radar.mjs && astro build && astro check && node scripts/check-build.mjs",
    "verify:radar": "node scripts/verify-radar.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.2",
    "astro-pagefind": "^2.0.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@pagefind/default-ui": "^1.5.2",
    "typescript": "^6.0.3"
  }
}
